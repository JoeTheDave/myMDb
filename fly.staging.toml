app = "mymdb-staging"
primary_region = "dfw"

[build]

[env]
  PORT = "3001"
  NODE_ENV = "production"
  FRONTEND_URL = "https://mymdb-staging.fly.dev"

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

[deploy]
  release_command = "sh -c 'cd /app/server && npx prisma migrate deploy --schema src/prisma/schema.prisma'"
