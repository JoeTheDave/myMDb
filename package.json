{
  "name": "mymdb",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "dev": "bash -c 'trap \"kill 0\" SIGINT SIGTERM EXIT; npm run dev --prefix server & npm run dev --prefix client & wait'",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
